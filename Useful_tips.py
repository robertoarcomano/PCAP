"""
Tips
math.hypot(2)
random.seed(10,2)
random.choices e sample [], k
__name__ RW
PYTHONPATH
OSError
(ex,)
Ex inheritance
super(this_class,self)
assert(1,2)
exception args in __str__
string.ascii_letters
' equivalent to "
[::-1]
1 == True
__iter__ keys on a dictionary
list.sort(reverse=True)
sorted(list,key=fn())
index(string, start, end)
assignment order
__new__ vs __init__
every class has its own class variable
methods can be defined outside the class
list is a superclass for all classes
TypeError: duplicate base class A
class variable static -> dynamic
Class data: .__dict__, vars()
method overloading: just the last one
__name__, __module__, __bases__ just for types
isinstance(obj, (items in OR))
closure: copy con write
"""

file = open("/etc/hosts", "r")
print(file.readlines(2))
file.close()
file.close()
file.close()


